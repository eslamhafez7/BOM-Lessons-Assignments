/*
BOM [What's BOM] L102
    - Introduction
        - window object is a browser window
        - window contain the document object
        - all global variables, objests and functions are members of window object
        - Test and console
    - What we can do with window
        - Open window 
        - Close window 
        - Move window
        - Resize window
        - Print Document
        - Run code after period of time once or more 
        - Fully control the URL
        - Save data inside browser to use later
*/

/*
let btn = document.querySelector("button");

btn.addEventListener("click", () => {
    newWindow = window.open("https://elzero.org", "_blank");
});

btn.addEventListener("click", () => {
    window.close();
}); // will not work security restriction.

window.moveTo(100, 100);
window.resizeTo(800, 600);
window.print();
setTimeout(function() {
    console.log("Code executed after 2 seconds");
}, 2000);
window.location.href = "https://www.example.com";
console.log(localStorage.setItem("username", "John"));

*/